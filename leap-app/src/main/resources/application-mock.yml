# Mock profile: disable OAuth2/OIDC auto-configuration.
# Authentication is handled by MockAuthController instead of Entra ID.
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.oauth2.client.servlet.OAuth2ClientAutoConfiguration
